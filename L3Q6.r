{
  # Create two lists.
  list1 <- list(1,2,3,4,5)
  list2 <- list("JAN","FEB","MAR", "APR", "MAY")
  
  # Merge the two lists.
  merged.list <- c(list1,list2)
  
  # Print the merged list.
  print(merged.list)
}