{
  # a matrix is symmetric
  # Creating a diagonal matrix
  pumm_261 <- diag(3)
  # Creating a matrix
  pumm_261a <- matrix(c(1, 2, 2, 3), 2)
  # Calling isSymmetric() function
  isSymmetric(pumm_261)
  isSymmetric(pumm_261a)
}